#pragma once

#include <stdlib.h>
#include <gmp.h>
#include "NumberBuffer.h"

void numberBuffer_test() {

    /*
    __mpz_struct *x = allocateAndInitializeNewGMPNumber();
    __mpz_struct *y = allocateAndInitializeNewGMPNumber();
    __mpz_struct *z = allocateAndInitializeNewGMPNumber();
    __mpz_struct *s = allocateAndInitializeNewGMPNumber();

    if (mpz_set_str(x, "5", 10) != 0)
        exit(EXIT_FAILURE);

    if (mpz_set_str(y, "178", 10) != 0)
        exit(EXIT_FAILURE);

    if (mpz_set_str(z, "500", 10) != 0)
        exit(EXIT_FAILURE);

    if (mpz_set_str(s, "1785", 10) != 0)
        exit(EXIT_FAILURE);

    chainedHashTableInsert(controlBlock->mainNumberBuffer, x, controlBlock->stringBuffer);
    chainedHashTableInsert(controlBlock->mainNumberBuffer, x, controlBlock->stringBuffer);
    chainedHashTableInsert(controlBlock->mainNumberBuffer, y, controlBlock->stringBuffer);
    chainedHashTableInsert(controlBlock->mainNumberBuffer, z, controlBlock->stringBuffer);
    chainedHashTableInsert(controlBlock->mainNumberBuffer, s, controlBlock->stringBuffer);

    if (chainedHashTableSearch(controlBlock->mainNumberBuffer, x, controlBlock->stringBuffer) != x)
        exit(EXIT_FAILURE);

    if (chainedHashTableSearch(controlBlock->mainNumberBuffer, y, controlBlock->stringBuffer) != y)
        exit(EXIT_FAILURE);

    if (chainedHashTableSearch(controlBlock->mainNumberBuffer, z, controlBlock->stringBuffer) != z)
        exit(EXIT_FAILURE);

    if (chainedHashTableSearch(controlBlock->mainNumberBuffer, s, controlBlock->stringBuffer) != s)
        exit(EXIT_FAILURE);
        */
}